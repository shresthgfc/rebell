[project]
name = "rebell"
version = "1.0.0"
description = "Multi-Agent Development Organization — 25 Claude sub-agents that brainstorm, debate, design, implement, test, and review software projects."
requires-python = ">=3.11"
dependencies = []  # No dependencies — uses `claude` CLI directly

[project.scripts]
rebell = "run:main"
